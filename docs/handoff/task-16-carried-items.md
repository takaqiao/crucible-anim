# Task 16 携带清单 —— 前 15 个任务累计的延后项

> **这份文件的地位**：Task 16 的简报（`task-16-brief.md`）只含**原始计划**写下的验收清单。
> 下面这些是执行过程中被判定「离线无法裁定、留给上机」的项，**不在简报里**。
> 两份都要做完，Task 16 才算完成。
>
> 前 15 个任务全部是离线判断：350 条测试证明的是「代码符合我对上游 API 的理解」，
> **不是**「屏幕上是对的」。Task 16 是整个项目唯一能证伪的环节。

## 一、只能靠人眼裁定的（视觉/听觉）

| # | 项 | 来源 | 怎么看 | 若不对怎么办 |
|---|---|---|---|---|
| C1 | `dark_*` 分支在暗色地图上糊没 | Task 9 | 6 个符文会精确命中低亮度描边版素材；色相旋转救不了亮度 | 换非 dark 分支，或加 `.opacity()`/发光补偿 |
| C2 | storm 与 frost 撞色 | Task 4 | 实际渲染多为「blue 底图 + 20° 色相滤镜」vs「blue + 0°」，只有滤镜级差异 | 给 storm 换素材分支而非改色相 |
| C3 | `poison.purple` 比 `green` 暗一档 | Task 11 | 暗底场景对比 | 同 C1 |
| C4 | `jb2a.ui.miss.white` 是**烘死的英文字** | Task 8 | 中文桌面上突然蹦英文 MISS | 产品决策：换纯符号素材或关掉 miss 提示 |
| C5 | 暴击震屏强度 | Task 13 | `intensity` 已改用 `gridUnits: true`（改前是 0.08 **像素**，肉眼不可见）；现在幅度对不对只能看 | 调 intensity 常量 |
| C6 | `.tint()` 是否真的染上色 | Task 13 | `play.mjs:415` 有下发、测试钉住「声明才下发」，但**染出来好不好看没人看过** | 调色或改走素材色分支 |
| C7 | 远程音效 ft 档位 | Task 9→10 | psfx ranged 按距离分 `.05/.30/.90ft`；90ft 版峰值在 1.50s，配贴脸攻击会听到**迟到的到达声** | 按实际格距选档 |
| C8 | `strike.ranged.draw` 三段时间轴烘在一条音轨 | Task 10 | 「箭到」声固定 0.8s 响、与 travel/impact 脱钩 | 拆音轨或接受 |
| C19 | 击杀血泊铺多大 | Task 15b | `death.kill` 的 `objectScale` 已按 Task 12 的新口径重推：本素材内容占画幅 0.837（逐帧实测，峰值帧 f27，判据 alpha≥26 的包围盒），取与同为地面层的 `aftermath.groundResidue`（内容占比 0.887 × objectScale 1.3 = **1.153 个格宽**）等可见跨度，得 1.153/0.837 = **1.38**。能离线定死的只到这里——「1.153 格这个目标本身合不合适」（血泊该从尸体底下往外溢出多少、`belowTokens` 之后还剩多少看得见）没人看过。**看法：杀一个 1 格敌人，看血泊有没有明显从 token 边缘漏出来；再杀一个 2 格大怪，看它是不是按体型成比例放大**（attachTo+bindScale 的预期行为） | 改 `armory/death.mjs` 的 `objectScale`；两条地面层要一起看，`aftermath.groundResidue` 的 1.3 同样从没上机看过 |

## 二、契约类（多客户端 / 生命周期，需两台客户端）

| # | 项 | 来源 | 怎么验 |
|---|---|---|---|
| C9 | persist 多客户端 flag 叠加 | Task 12→13 | Sequencer `.locally(true)` 只挡 socket、**仍会 setFlags**。已加 `!data.local` 处理（`play.mjs:429`）。**验法：GM+1 玩家在线，加光环状态，反复 F5 三次，看光环有没有一层层变亮** |
| C10 | `resyncPersist` 三种情形 | Task 15 | ①重载 ②换场景 ③玩家迟到加入——各自看持久光环是否恰好一份、不多不少 |
| C11 | 重放菜单 | Task 15 | 右键聊天卡→菜单项出现→点击能重放。**v14 键名刚改过（`label`/`visible`/`onClick`），这是回归确认** |
| C12 | `/canim-preview` 往返 | Task 15 | 聊天命令 + `game.modules.get("crucible-anim").api.preview()` 两条路径 |
| C20 | 击杀爆发只放一次 | Task 15b | 打死一个敌人 → 血泊只在**死亡那一刻**出现一次，且排在那记攻击的动画**之后**（走的是 persist 那套让路）。然后：切场景再切回来、把尸体拖到另一张图、F5 重载——**都不该再喷一次血**。多客户端下每台机器各放一次（钩子在每个客户端本地触发，不走 socket）。反向：`dead` 不该再有持久光环（Foundry 自带 overlay） |

## 三、已知的既有行为（确认是否可接受，非缺陷）

- **C13** 4 条投掷类非攻击动作解析出 0 条 impact cue（HEAD 既有行为，`generic.impact` 让位让过头）
- **C14** `strike.ranged.draw` 视觉层缺失——待你确认归 travel 槽还是后补
- **C15** `jb2a.cast_generic.01` 第 4 色分支实为 `dark_purple/dark_red/blue/yellow`，
  ASSET-NOTES 原文隐含写成 `.../green`（不影响功能，兜底走最近色+色相补偿）

## 四、会静默过期的东西（不是现在的 bug，是将来的）

- **C16** `PALETTE.lab` 是**测量常量**，素材一旦更新就静默过期；
  当前 MIN_DELTA_E 余量仅 **0.7–0.9**，几乎贴着阈值
- **C17** `docs/ASSET-NOTES.md` 的备注列是**带证据的参考，不是定论**
  （两轮复核各自订正过；93 行里第二轮仍在 85 行未抽样区找到问题）
- **C18** Sequencer 在 `duration:null + playbackRate:2` 下的墙钟时长，纯离线推演

## 五、最重要的一项

**#12（原始清单）：确认 Crucible 原生已实现的 24 组法术组合仍走它自己的粒子系统，
本模组全程不介入。**

这是「只补空缺」这条根本设计的最终验证。判据是 `configureVFXEffect` 返回非 null ⟺
本模组不出手。如果这条塌了，整个 V1 的定位就错了——比上面任何一条都严重。
